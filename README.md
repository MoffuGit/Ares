the zig project it's going to get used by the desktop app(electrobun) and
for the cli app (opentui)

lets update the bus and ts callback function,
after that, lets check how to send the path to the load settings
function, before using this function, we should check where we are going to create
the monitor (probably on the core class),
