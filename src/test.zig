const datastruct = @import("datastruct/mod.zig");
const Element = @import("element/mod.zig");
const Window = @import("Window.zig");
const split = @import("split/mod.zig");

test {
    _ = datastruct;
    _ = Element;
    _ = Window;
    _ = split;
}
